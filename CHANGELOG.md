# 版本记录

## 未发布

### 新增

- 新增 1400 页面 TAF 弹幕识别，标准化 UID、昵称、正文和消息标识。
- 新增当前账号与白名单配置，为后续远程控制和趣味互动预留分模块权限。
- 新增有界弹幕状态：最近发言人最多 100 人，授权记录最多 200 条；未授权观众正文不保存、不打印。
- 新增趣味互动·文本轮播：本机导入 UTF-8 TXT（≤5 MiB，小说、话术、台词皆可），按自然段与标点拆条逐条连播，支持循环回卷。
- 新增本地文本库 `data/novels/`：随机 ID 存储、SHA256 一致性校验、路径穿越防护，退出 Git 跟踪。
- 新增轮播播放器状态机（待机/播放/暂停/播完/异常）：单条在途、低优先级入队、失败不重试顺序推进、重启后保留进度但降级为暂停。
- 新增六条 `LU 轮播` 指令：开始、暂停、继续、停止、下一条、状态（旧关键词「小说」兼容）；白名单需单独勾选“允许控制轮播”。

### 调整

- WebUI 重构为固定核心状态栏，以及总览、自动场控、远程控制、趣味互动、运行记录五个分页。
- 运行状态增加 TAF、1400 监听和发送队列数量；程序自身弹幕会从授权输入中排除。
- 关闭 WebUI 状态轮询产生的 Uvicorn 访问日志，避免控制台每 3 秒刷屏。
- 新增严格的 `LU 开启/关闭 发送/欢迎/感谢` 一级场控指令；“感谢”聚合控制礼物及全部非礼物感谢。
- 白名单按模块权限执行 LU 指令；关闭发送时立即清空待发队列，WebUI 会在新指令后同步开关状态。
- 统一弹幕队列增加三级优先级（高：指令反馈；普通：欢迎/感谢；低：轮播），同级先进先出，均不绕过全局 CD。
- 弹幕文本上限 28 字（虎牙输入框上限 30 字）：超长拒绝入队并记日志，不做截断。
- 白名单旧字段 `allow_interaction` 演进为 `allowed_interactions` 列表；旧 `true` 不自动授予轮播权限，需重新明确授权。
- 轮播进度只记条目序号：修改每条字数或更换文本时进度归零重新开始。
- `LU 关闭 发送` 时轮播同步暂停并清除在途条；重新开启后不自动恢复。

## 1.0.0 - 2026-08-29

### 新增

- 作为首个正式版本发布，项目代码采用 MIT License。
- 新增贵族感谢：识别 1001 开通/升级、续费、贵族名称和月份。
- 新增超粉感谢：10079 普通超粉与 2001231 超粉PLUS 使用独立开关和统一开通文案。
- 礼物感谢新增“单件礼物低于多少元不感谢”，金额支持小数元。
- 非礼物感谢在 WebUI 中合并为守护、超粉、贵族三个纵向独立模块。
- 发布检查新增 WebUI 入口、缺失产物和旧哈希产物校验。

### 调整

- 本机 `config/app.json` 退出版本控制，仓库只保留全部关闭的安全示例配置。
- 生产事件改为页面 TAF-only，彻底移除 WebSocket/JCE 业务兜底和跨通道去重。
- TAF 订阅扩展为 1001 / 6110 / 6501 / 6540 / 10079 / 2001231。
- 守护只接受身份、昵称、开通/升级动作和已知守护名称都完整的 6540；感谢不再关心 V 等级、天数或类型枚举。
- 礼物默认文案调整为 `感谢{nick}送的{count}个{item_name}`。
- 弹幕发送间隔从“发送按钮成功点击”后开始计算；发送失败不占用 CD、不自动重试。
- WebUI 调整为两列弹性布局，进场与礼物占满首行，非礼物感谢和弹幕发送横跨整行。

## 0.1.0 - 2026-08-28

首个可试用版本：Windows 一键环境、网页配置、虎牙登录、单房间挂房、进场欢迎、礼物/守护感谢、统一弹幕队列、断线恢复和日志轮转。
