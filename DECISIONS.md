# 实现决定

| 编号 | 决定 |
| --- | --- |
| D-001 | 产品只支持 Windows 本地、单账号、单直播间。 |
| D-002 | 使用 Python + FastAPI + Vue + Playwright 持久化 Profile；登录和验证码由用户在可见浏览器完成。 |
| D-003 | 自动弹幕只走直播间页面输入框，所有功能统一调用 danmaku 队列。 |
| D-004 | CD 从发送按钮成功点击后开始；失败记录日志但不重试，避免重复刷屏。 |
| D-005 | 生产业务事件只接受页面 `addTafListener` 的结构化回调，WebSocket/JCE 不作为备用来源。 |
| D-006 | WebSocket/JCE 仅用于 `validation/` 中发现 URI、保存样本和分析字段，不得进入产品总线。 |
| D-007 | welcome、gift_thank、guard_thank、superfan_thank、noble_thank 相互独立，只共享 danmaku。 |
| D-008 | 6111 普通用户不进入业务，也不逐条打印。 |
| D-009 | 礼物金额使用 6501 的 `lPayTotal`；合计门槛与单件门槛独立，单件金额为合计金额除以数量。 |
| D-010 | 礼物优先按 `strPayId` 去重；0 元礼物只记录、不感谢。 |
| D-011 | 守护仅接受 6540 中完整的身份、昵称、横幅动作和已知守护名称；动作仅为开通/升级，名称仅为初爱/超级/至尊守护。 |
| D-012 | 1400 系统句和 WebSocket 6540 不触发守护业务；守护 V 等级、天数和类型枚举不进入领域合同。 |
| D-013 | 贵族 1001 的 `openFlag=1` 统一为开通/升级，`openFlag=2` 为续费；月份优先读取 payMonth，缺失时由 openDays/30 还原。 |
| D-014 | 普通超粉使用 10079，超粉PLUS 使用带 Plus 标记的 2001231；开通和续费在产品文案中统一写作开通。 |
| D-015 | 同步 Playwright 只在工人线程操作；FastAPI 只发送登录、启动和停止命令。 |
| D-016 | 登录换页时选择最新存活页面，TargetClosed 后重试一次。 |
| D-017 | 公开默认配置为空房间且所有自动功能关闭。 |
| D-018 | 新业务流程固定为：WS 探针发现 → 样本分析 → TAF 订阅验证 → 字段合同与测试 → 产品注册。 |

DOM 只用于发送弹幕，不用于识别业务事件；不自建弹幕客户端，也不绕过平台限制。
